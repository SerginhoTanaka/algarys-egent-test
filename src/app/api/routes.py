from typing import Any

from app.core.logging import get_logger

logger = get_logger(__name__)


def register_routes(app: Any):
    logger.debug("register_routes called")
    # Add route registration here when API is wired up.
    return
